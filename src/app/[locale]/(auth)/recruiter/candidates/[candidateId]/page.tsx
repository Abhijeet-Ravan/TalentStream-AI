import { RecruiterPlaceholderPage } from '@/features/recruitment/RecruiterPlaceholderPage';

export default function RecruiterCandidateDetailPage() {
  return (
    <RecruiterPlaceholderPage
      title="Candidate Details"
      description="Inspect a candidate profile, application context, and future AI match insights."
    />
  );
}
