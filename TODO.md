# TODO - Recruiter sidebar Server/Client boundary fix

- [x] Add `"use client"` directive to `src/features/recruitment/layout/RecruiterSidebar.tsx` so Lucide icon components and navItems are resolved inside Client Components.
- [ ] Re-run `npm run lint`.
- [ ] Re-run `npm run check:types`.
- [ ] Re-run `npm run check:deps`.

- [ ] Smoke test: visit `/recruiter/candidates` and confirm no RSC serialization error and active nav state works.

